// Functions to set element styles
export const setElementWidth = (element, value)=>{
    element.style.width = `${value}px`;
}

export const setElementRightProperty = (element, value) => {
    element.style.right = `${value}px`;
}

export const setElementHeight = (element, value) => {
    element.style.height = `${value}px`;
}